# Changelog · 个人 UI 定制版（fork）

本文件记录相对上游 [finlater/weread.koplugin](https://github.com/finlater/weread.koplugin) v1.4.2 的定制变更，均位于 `ui-tweak` 分支。上游自身的变更见仓库根 [CHANGELOG.md](CHANGELOG.md)；fork 用法与边界见 [README.fork.md](README.fork.md)。

## [1.4.2-fork.3] - 2026-09-09

### 新增

- **可复用全屏宿主（FullscreenHost）** — 书架页与阅读统计页共用同一套宿主基础设施：SimpleUI 状态栏 / 底部导航栏让位、dock（图标与标签自动引用 SimpleUI 自己的注册表，换图标、加项目自动跟随）、前光手势（左缘 + 双指）、顶部呼出原生菜单、页内电源菜单、收到 KOReader Exit 广播自动退场。
- **阅读统计页接入宿主** — 从书架 dock 直达统计页（不再闪主页；书架在数据就绪后才让位），或在任意 SimpleUI 界面 dock 打开统计 = 统计页带状态栏与底部导航栏外壳；在阅读器内打开统计则保持官方全屏原样。
- **dock 图标 / 项目灵活化** — dock 各项图标与名称直接调用 SimpleUI 的 QA 注册表解析：在 SimpleUI 里换图标、加自定义快捷项、改名，书架 dock 自动跟随；新增的 dock 自定义项（非 weread 入口）可正常点击转发。
- **dock「当前页」判定修正** — 只有真正指向 weread 插件的 dock 项才高亮为当前页；其余 custom 项按普通项处理。

### 变更

- 书架页 dock / 让位 / 手势 / 电源菜单的实现迁移到 FullscreenHost（架构层提取，行为不变）；dock 图标映射表从写死改为调用 SimpleUI 注册表。

### 移除

- 无（本 fork 均为增量定制）。

## [1.4.2-fork.2] - 2026-09-08

### 新增

- **顶部边缘下拉原生菜单** — 书架顶部点击或从顶部下滑，直接呼出 KOReader **原生**主菜单，行为与 KOReader 其它页面完全一致（上次打开的标签页自动记忆）。
- **Kindle 式菜单遮罩（KOReader 全局，不只书架页）** — 弹出 KOReader 任意原生菜单（书架 / 文件管理 / 阅读页）时，菜单下方页面以 Kindle 同款棋盘遮罩压暗（4px 黑白格 1:1），菜单关闭即还原。⚠ 维护注意：此功能为全局行为，由插件在启动时的一次性运行时补丁实现——不修改任何 KOReader 本体文件（升级无冲突），但**禁用 / 卸载本插件即失效**。
- **前光快捷手势** — 左缘上下滑、或双指上下滑直接调整前光（亮度曲线与开关边界与 KOReader 一致），并以原生通知条反馈当前亮度。

### 变更

- **顶部让位带可重绘** — 书架层调整全屏声明语义，使 SimpleUI 状态栏所在的顶部带在通知 / 遮罩等浮层移除后能被真正重绘（修复此前浮层移除后在状态栏处残留残影的根因）。⚠ 该改动改变了书架层向文件管理器层的脏区穿透行为，KOReader 大版本升级后需复核。

## [1.4.2-fork.1] - 2026-09-08

### 新增

- **书架页底部 dock 与 SimpleUI 一致** — 书架页底部新增与 SimpleUI 相同的导航栏（书库 / 主页 / 电源等，遵循 SimpleUI 的 dock 配置显示）。在 weread 页内即可点按离开或切换，weread 项显示为当前页激活状态；点书库/主页等直达 SimpleUI 对应屏，不再需要先退回。
- **页内电源菜单** — 点击 dock 的电源项，在 weread 页内直接弹出电源菜单（重启 / 休眠 / 退出），无需离开当前页；选择退出时数据正常保存。

### 变更

- **为 SimpleUI 状态栏与导航栏让位** — 书架页顶部与底部留出空间，SimpleUI 的顶部状态栏与底部导航栏在本页可见，标题栏相应下移，两界面切换不再跳变。
- **封面框线紧贴封面图** — 封面模式的细框改为紧贴封面图四周，各种比例的封面都完整显示，不再留白边、不拉伸不裁切；「新书 / 未读」角标随之贴合封面框。
- **书架头部合并为一行** — 「书籍 / 公众号」改为左侧紧凑标签（不再各占半屏），排序 / 筛选 / 搜索 / 刷新并入同一行、靠右显示。

### 移除

- **标题栏关闭按钮（✕）** — 顶部标题栏不再显示关闭按钮，离开 weread 改由底部 dock 完成。
