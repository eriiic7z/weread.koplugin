# Changelog · 个人 UI 定制版（fork）

本文件记录相对上游 [finlater/weread.koplugin](https://github.com/finlater/weread.koplugin) v1.4.2 的定制变更；上游自身的变更见仓库根 [CHANGELOG.md](CHANGELOG.md)，用法与边界见 [README.fork.md](README.fork.md)。

格式依据 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，版本号遵循 [Semantic Versioning](https://semver.org/)。

## [1.4.2-fork.9] - 2026-09-22

### 变更

- **两页封面网格改由同一个设置控制** — 行列数统一跟随 KOReader 的「每页项目数」（显示模式设置里那一项）：改一次，书架页与本地书库同时变化；封面尺寸按行列与可用面积自适应，保持原比例填满「格子减去书名 / 作者行」的区域。
- **两页封面网格对齐** — 外边距与标题分隔线对齐、封面之间的间隔统一；两页网格区域面积一致、封面尺寸一致、顶端对齐（两页与控制行的距离允许不同，由留白吸收）。
- **网格随标题栏尺寸档位整体移动** — 切换 SimpleUI 的「标题栏 → Button Size」时，本地书库的封面网格随之上下平移（此前只在原地缩放），两页顶端保持对齐。

### 修复

- **修复重启后本地书库封面墙右边距偶发归零** — 此前偶发出现，现在稳定。
- **截图等 KOReader 手势在两页恢复可用** — 书架页与统计页现在把手势交回 KOReader 处理，你在 KOReader 里配置的手势（如双指握合截图）在两页同样生效。
- **「每页项目数」改完立即生效** — 此前需先切换页面，或需在设置里再点一次「关闭」才看到变化（重启后的第一次同样如此）。

## [1.4.2-fork.8] - 2026-09-14

### 新增

- **长按标题栏打开该栏设置** — 书架页与统计页长按标题区域（标题、分隔线、标签 / 操作行、空白处）即打开 SimpleUI 的「标题栏」设置窗口，与本地书库行为一致；窗口标题跟随界面语言。
- **分页器可点击输入页码** — 书架页分页器中间的「x/y」点击后弹出页码输入框（KOReader 原生输入框），输入页码确认即跳转；本地书库分页器维持原有行为。
- **分页器可长按打开分页栏设置** — 书架页与本地书库长按分页器的页码或四个箭头（松手生效）即打开 SimpleUI 的「分页栏」设置窗口。

### 变更

- **三处标题字号统一加大两号** — 书架、统计、本地书库标题由 26 号改为 28 号。
- **头部控件跟随 SimpleUI 的标题栏尺寸档位** — 书架 / 公众号标签、刷新 / 搜索 / 排序 / 筛选、统计页标签随「紧凑 / 默认 / 大」三档同步缩放；与分隔线对齐的左右边距不随档位缩放。

### 修复

- **改标题栏尺寸立即生效、页面不再错位** — 此前需切换页面才生效，且页面会跳到屏幕最上方、状态栏被遮住；现在与上游同一步骤内重排页面，并保留 SimpleUI 的顶部偏移，无需再切页。
- **修复长按分页器导致 KOReader 闪退** — 现在长按分页器不再导致 KOReader 退出。

## [1.4.2-fork.7] - 2026-09-14

### 新增

- **底部导航栏翻页箭头（navpager）支持** — SimpleUI 的 navpager 打开时，书架页与统计页底部导航栏两端的箭头可用：点击翻页（统计页切换周期），长按不松手跳到首页 / 末页（统计页长按右箭头直达最新周期）。

### 变更

- **本地书库头部改为工具行** — 返回 / 搜索 / 浏览 / 菜单四个操作图标移到标题分隔线下方同一行，封面网格整体下移，路径副标题与图标同行居中，与书架页一致。
- **三处标题规格统一** — 书架、统计、本地书库共用同一套标题字号、分隔线位置与行距；进入子文件夹再返回上级时标题不再横移一像素。
- **navpager 开关联动页内导航** — navpager 打开时页面内不再显示分页行（书架 / 公众号）与周期行（统计），改由底部导航栏箭头承担；关闭后恢复页内导航。

### 改进

- **页面切换更干净** — 切换后不再有页面残留在后台。
- **标题规格改为单一来源** — 书架 / 统计 / 本地书库的标题规格集中定义，后续微调只改一处（无视觉变化）。

### 修复

- **修复 navpager 打开时切换页面的屏幕闪烁** — 切页不再出现多余闪烁，箭头状态也正确。
- **修复切换后界面卡住、点击无反应** — 点 dock 项或切页不再停在旧画面。
- **修复底部导航栏误触相邻项** — 点击某一项不再跳到相邻项。
- **修复 SimpleUI 主页左右箭头高亮但点击无反应** — 主页上箭头此前只能亮起、点不动；现在可正常切到上一屏 / 下一屏。
- **修复统计页长按右箭头无反应** — 现在可长按右箭头直达最新周期。

## [1.4.2-fork.6] - 2026-09-10

### 变更

- **书架 / 公众号分页器改为复刻本地书库** — 图标与 `x/y` 页码、按钮占位与间距与本地书库一致；点击不再需要精准对准。
- **本地书库分页器同步同规格** — 与书架页统一到同一套规格（位置与高度不变）；不改动 SimpleUI 源文件，升级不丢。
- **统计页** — 周月年总 tab 上缘与书架标题分隔线对齐；周期导航去掉无功能的装饰箭头；标题栏底部内距与书架同参。
- **补丁结构整理** — 多个补丁模块合并为单个模块，维护时只改一处（无视觉变化）。
- **页面尺寸与文案改为可配置** — 让位带高度优先取 SimpleUI 的值；页内电源菜单与前光通知文案可由调用方覆盖（缺省保持原中文）。

## [1.4.2-fork.5] - 2026-09-09

### 变更

- **封面书名与作者对齐本地书库** — 封面下书名 15px 加粗、作者 12px 常规（本地书库封面墙同规格）；行距收窄至与封面框底间距一致。
- **书架页视觉细节统一** — 标题「微信读书」加大至 26（本地书库同系字号）；标题下分隔线、封面网格、“书籍 / 公众号”tab 组统一 24px 边距体系；tab 点按高亮改直角小矩形、激活指示线 1px 与 dock 分隔线同粗。
- **修复 dock 点击闪屏** — 书架页点 weread 图标不再闪主页、点统计图标不再闪本地书库；dock 高亮条恢复。
- **公众号列表页** — 行字号/左右边距对齐 24px 体系；右侧副信息独立字号并浅灰；每页行数改为按可视高度自动装行（消除列表与页码间的空行）；页码导航改为居中窄条。
- **控制条比例统一** — “书籍/公众号”tab、右侧操作按钮、页码与上下周期导航统一“按钮高 = 文字行高 + 1px 内距”的比例规则（小字配矮背景）。
- **阅读统计页对齐书架** — 标题同款字号与高度、去 ✕、去标题线（tab 直接贴标题下缘）；周月年总 tab 收窄至与卡片同宽；卡片左右 24px 边距与封面网格同宽；上/下周期导航改居中窄条；滚动条 0.25 灰、宽度减半并左移留边。

## [1.4.2-fork.4] - 2026-09-09

### 变更

- **顶部标题对齐本地书库** — 标题由「微信读书书架」改为「微信读书」；字号与顶部内距与本地书库标题一致，标题与本地书库同高（整页相应上提）。
- **标题下分隔线统一** — 顶部标题下改用与底部 dock 分隔线同款的自绘分隔线（1px 浅灰，左右各 24px 内缩），替换原 TitleBar 内置底线。
- **封面网格左右留白** — 封面网格整体左右各收进 24px，与 dock 分隔线 / 标题分隔线的左右内缩对齐。
- **「书籍 / 公众号」tab 对齐与外观** — tab 组左边距对齐 dock 分隔线（24px）；激活指示横线改为 1px，与 dock 分隔线同粗；点按高亮改为直角矩形并缩小（按钮不再带背景——KOReader 对带背景的按钮会强制圆角高亮），与标题分隔线、下方指示线之间各留空隙。
- **右侧操作顺序与留白** — 操作顺序由 排序 / 筛选 / 搜索 / 刷新 调整为 刷新 / 搜索 / 排序 / 筛选；模块右边距 28px，与左侧 tab 留白视觉对称。
- **封面下书名对齐本地书库** — 封面下书名改用与本地书库列表行文字相同的字号与字重（常规）。

## [1.4.2-fork.3] - 2026-09-09

### 新增

- **书架页与统计页共用同一套全屏外壳** — 两页共用同一套实现：SimpleUI 状态栏 / 底部导航栏让位、dock（图标与标签自动引用 SimpleUI 自己的注册表，换图标、加项目自动跟随）、前光手势（左缘 + 双指）、顶部呼出原生菜单、页内电源菜单、退出 KOReader 时自动退场。
- **阅读统计页接入宿主** — 从书架 dock 直达统计页（不再闪主页；书架在数据就绪后才让位），或在任意 SimpleUI 界面 dock 打开统计 = 统计页带状态栏与底部导航栏外壳；在阅读器内打开统计则保持官方全屏原样。
- **dock 图标 / 项目灵活化** — dock 各项图标与名称直接跟随 SimpleUI 的注册表：在 SimpleUI 里换图标、加自定义快捷项、改名，书架 dock 自动跟随；新增的 dock 自定义项（非 weread 入口）可正常点击转发。
- **dock「当前页」判定修正** — 只有真正指向 weread 插件的 dock 项才高亮为当前页；其余 custom 项按普通项处理。

### 变更

- 实现结构整理 — 书架页与统计页共用同一套宕主实现（行为不变）；dock 图标映射改为调用 SimpleUI 注册表。

## [1.4.2-fork.2] - 2026-09-08

### 新增

- **顶部边缘下拉原生菜单** — 书架顶部点击或从顶部下滑，直接呼出 KOReader **原生**主菜单，行为与 KOReader 其它页面完全一致（上次打开的标签页自动记忆）。
- **Kindle 式菜单遮罩（KOReader 全局，不只书架页）** — 弹出 KOReader 任意原生菜单（书架 / 文件管理 / 阅读页）时，菜单下方页面以 Kindle 同款棋盘遮罩压暗（4px 黑白格 1:1），菜单关闭即还原。注意：此功能为全局行为，**禁用或卸载本插件即失效**。
- **前光快捷手势** — 左缘上下滑、或双指上下滑直接调整前光（亮度曲线与开关边界与 KOReader 一致），并以原生通知条反馈当前亮度。

### 变更

- **修复状态栏处残影** — 通知 / 遮罩等浮层移除后，状态栏区域不再残留旧画面。

## [1.4.2-fork.1] - 2026-09-08

### 新增

- **书架页底部 dock 与 SimpleUI 一致** — 书架页底部新增与 SimpleUI 相同的导航栏（书库 / 主页 / 电源等，遵循 SimpleUI 的 dock 配置显示）。在 weread 页内即可点按离开或切换，weread 项显示为当前页激活状态；点书库/主页等直达 SimpleUI 对应屏，不再需要先退回。
- **页内电源菜单** — 点击 dock 的电源项，在 weread 页内直接弹出电源菜单（重启 / 休眠 / 退出），无需离开当前页；选择退出时数据正常保存。

### 变更

- **为 SimpleUI 状态栏与导航栏让位** — 书架页顶部与底部留出空间，SimpleUI 的顶部状态栏与底部导航栏在本页可见，标题栏相应下移，两界面切换不再跳变。
- **封面框线紧贴封面图** — 封面模式的细框改为紧贴封面图四周，各种比例的封面都完整显示，不再留白边、不拉伸不裁切；「新书 / 未读」角标随之贴合封面框。
- **书架头部合并为一行** — 「书籍 / 公众号」改为左侧紧凑标签（不再各占半屏），排序 / 筛选 / 搜索 / 刷新并入同一行、靠右显示。

### 移除

- **标题栏关闭按钮（✕）** — 顶部标题栏不再显示关闭按钮，离开 weread 改由底部 dock 完成。
